# Optional credentials for manual API/gated-model sections. Copy to .env when needed.
# Never put real keys in a notebook or commit them to Git.
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
OPENROUTER_API_KEY=
HF_TOKEN=
