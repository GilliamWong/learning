# Experiments

Copy `templates/Experiment.ipynb` here for each new investigation. Use a descriptive filename.
Keep the question, model and data choices, predictions, controls, results, and limitations together.
These personal experiment files are excluded from Git by default. You can deliberately add work
you want to version when you are ready.
